<td><%= jersey_with_number file_row %></td>
<td><%= link_to file_row.player_name, [file_row], 'data-toggle' => 'modal', 'data-target' => "#show_player_#{file_row.id}_info" %></td>
<td><%= file_row.position.text %></td>
<td><%= file_row.value %></td>
<td><%= file_row.respond_to?(:points) ? file_row.points : file_row.player.league_stats(:points,@current_league) %></td>
<td><%= country_flag file_row %></td>

<div class="modal hide" id="show_player_<%= file_row.id %>_info">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal">×</button>
    <h3><%= file_row.player_name %></h3>
  </div>
  <div class="modal-body">
  </div>
  <div class="modal-footer">
    <a href="#" class="btn" data-dismiss="modal"><%= t('.close') %></a>
  </div>
</div>
