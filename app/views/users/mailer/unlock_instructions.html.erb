<p><%= t '.hello', :name => @resource.name %></p>

<p><%= t '.your_account_as_been_locked' %></p>

<p><%= link_to t('.unlock_account'), unlock_url(@resource, :unlock_token => @resource.unlock_token) %></p>