language: ruby
rvm:
  - 1.9.3
# uncomment this line if your project needs to run something other than `rake`:
# script: bundle exec rspec spec

before_script:
  - psql -c 'create database onleague_test;' -U postgres
  - bundle exec rake db:test:preapare

branches:
  only:
    - master
    - development